# Posting console

Show complete variants and exact media. Each POST button saves the selected option, immutable exact text and asset list, publicationAuthorized=true, authorization provenance and time. POST authorizes final publication without another confirmation. Poll server state and display queued, awaiting_browser, posting, submitted, unknown, posted or failed honestly. Lock posting/submitted/unknown/posted against duplicate clicks. Never label queue acceptance as posted. Show current browser/dispatch blockers. An idle local HTML page cannot invoke the agent; do not claim it can.

Current handoff: POST saves the selected option; the user then says post in chat. Process every authorized unpublished platform through the Browser plugin without repeated approval. Device sessions and local queues do not sync automatically. See post-runner/SKILL.md for execution and reconciliation.
